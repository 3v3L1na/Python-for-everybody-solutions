fruit = 'strawberry'  #define variable "fruit"
index = len (fruit) -1 #define function "len" and how it has to work backwards using -1
while index >=0:       #commence "While" loop. It has to be equal or more than 0 because the task says to go backwards to the first character
    letter=fruit[index]
    print (letter)
    index-=1
    
fruit ='kiwi'
for char in fruit:
    print (char)
    
    #These are the traversals throuhg a string with a loop